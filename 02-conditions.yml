-  name: conditions
   hosts: local
   connection: local
   vars:
    IS_LIVE: true
    tasks:
     - name: check live or not
       ansible.builtin.debug:
        msg: "yes it'slive"
       when: IS_LIVE
      