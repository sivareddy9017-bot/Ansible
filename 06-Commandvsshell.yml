- name : Command line module
  hosts: local
  Connection: local 
  tasks:
  - name: command module test
  #ansible.builtin.command: cat /ectc/passwd | grep root
  ansible.builtin.shell: cat /ectc/passwd | grep root
  register: user_output  # register is special variable of Ansible script to store these vaules data

- name: print output
  ansible.builtin.debug:
    msg: "{{ users_output }}"

- name: command module test
  #ansible.builtin.command: cat /ectc/passwd | grep root
  ansible.builtin.shell: ls -ltr > files.txt
  register: files_output

- name: print output
  ansible.builtin.debug:
    msg: "{{ files_output }}"
